{
  "name": "browser-designs",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @browser-designs/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "screenshot": "node scripts/screenshot.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.1"
  }
}
